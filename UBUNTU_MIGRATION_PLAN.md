# 🚀 План миграции Shorts News на Ubuntu Server

## 📊 Анализ существующего проекта

На основе анализа вашего работающего проекта **Telegram-to-YouTube Shorts** создана аналогичная структура развёртывания для **Shorts News**.

### Ключевые паттерны из работающего проекта

1. **Systemd сервис** (`telegram-shorts.service`)
   - Автозапуск при старте системы
   - Автоматический перезапуск при сбое
   - Логирование через journalctl

2. **Скрипты управления**
   - `install_ubuntu.sh` — автоматическая установка
   - `update_bot.sh` — обновление с Git + бэкапы
   - `view_logs.sh` — удобный просмотр логов

3. **Структура директорий**
   - `/home/dzianis/<project_name>/` — основная директория
   - `.venv/` — виртуальное окружение
   - `resources/`, `outputs/`, `logs/` — рабочие папки

4. **Управление**
   - Git для обновлений
   - Автоматические бэкапы перед обновлением
   - Возможность отката (rollback)

---

## 📦 Созданные файлы для развёртывания

### 1. `deploy/install_ubuntu.sh`
Автоматическая установка всех зависимостей и настройка systemd сервиса.

**Что делает:**
- Обновляет систему
- Устанавливает Python 3, pip, venv, git, ffmpeg, chromium
- Создаёт виртуальное окружение
- Устанавливает Python зависимости из `requirements.txt`
- Создаёт рабочие директории
- Настраивает systemd сервис `shorts-news.service`
- Создаёт шаблон `.env` файла

### 2. `deploy/systemd-service.template`
Шаблон systemd сервиса с security настройками.

**Особенности:**
- Автозапуск при старте системы
- Автоматический перезапуск при падении (RestartSec=10)
- Security hardening (NoNewPrivileges, PrivateTmp, ProtectSystem)
- Логирование через journalctl

### 3. `deploy/update_bot.sh`
Скрипт для управления обновлениями и обслуживания.

**Команды:**
- `update` — обновление с Git + автобэкап
- `restart` — перезапуск сервиса
- `status` — статус сервиса и Git
- `logs` — просмотр логов в реальном времени
- `backup` — создание бэкапа (последние 10 хранятся)
- `rollback` — откат к последнему бэкапу

### 4. `deploy/view_logs.sh`
Удобный просмотр логов из разных источников.

**Режимы:**
- `live` — systemd journalctl в реальном времени
- `file` — файловые логи
- `errors` — только ошибки
- `today` — логи за сегодня
- `grep PATTERN` — поиск по паттерну

### 5. `UBUNTU_DEPLOYMENT.md`
Полная документация по развёртыванию:
- Быстрая установка
- Управление сервисом
- Отладка и troubleshooting
- Безопасность
- Мониторинг

### 6. `deploy/QUICK_DEPLOY.md`
Краткое руководство с чек-листами и быстрыми командами.

---

## 🔄 Workflow развёртывания

### Первоначальная установка

```bash
# 1. На сервере
cd /home/dzianis
git clone <your-repo-url> shorts_news
cd shorts_news

# 2. Запуск установки
chmod +x deploy/install_ubuntu.sh
./deploy/install_ubuntu.sh

# 3. Настройка конфигурации
nano .env  # Добавить API ключи

# 4. Копирование ресурсов
# - resources/music/*.mp3
# - resources/fonts/*.ttf
# - config/client_secret.json

# 5. Запуск
sudo systemctl enable shorts-news
sudo systemctl start shorts-news
sudo systemctl status shorts-news
```

### Ежедневное использование

```bash
# Просмотр статуса
./deploy/update_bot.sh status

# Просмотр логов
./deploy/update_bot.sh logs

# Перезапуск (если нужно)
./deploy/update_bot.sh restart
```

### Обновление кода

```bash
# Автоматическое обновление с Git
./deploy/update_bot.sh update

# Создаст бэкап, обновит код, перезапустит сервис
```

### При проблемах

```bash
# Откат к предыдущей версии
./deploy/update_bot.sh rollback

# Просмотр ошибок
./deploy/view_logs.sh errors

# Проверка статуса
sudo systemctl status shorts-news
```

---

## 🔑 Ключевые отличия от Windows версии

| Аспект | Windows | Ubuntu |
|--------|---------|--------|
| Запуск | `start_monitor.bat` | systemd сервис |
| Автозапуск | Task Scheduler | systemd enable |
| Логи | Файлы в `logs/` | journalctl + файлы |
| Обновление | Ручное | `update_bot.sh update` |
| Chrome | Обычный Chrome | chromium-browser + Xvfb |
| Виртуальная среда | `venv\Scripts\activate` | `source venv/bin/activate` |
| Пути | `\` (backslash) | `/` (forward slash) |

---

## ⚠️ Важные моменты

### 1. Selenium на сервере без GUI

Ubuntu сервер не имеет графического интерфейса, поэтому нужен **Xvfb** (X virtual framebuffer):

```bash
sudo apt install xvfb
```

В systemd сервисе уже добавлено:
```ini
Environment=DISPLAY=:99
```

### 2. Chrome/Chromium

На сервере используется `chromium-browser` вместо Chrome:

```bash
sudo apt install chromium-browser chromium-chromedriver
```

В коде Selenium автоматически найдёт chromedriver.

### 3. Пути к файлам

Проверьте что все пути в коде относительные или используют `Path` из `pathlib`:

```python
# ✅ Хорошо
from pathlib import Path
config_path = Path('config/config.yaml')

# ❌ Плохо
config_path = 'config\\config.yaml'  # Windows-style
```

### 4. FFmpeg

FFmpeg устанавливается автоматически скриптом `install_ubuntu.sh`:

```bash
sudo apt install ffmpeg
```

### 5. Переменные окружения

`.env` файл должен быть настроен после установки:

```env
TELEGRAM_BOT_TOKEN=...
TELEGRAM_CHANNEL_ID=...
GEMINI_API_KEY=...
```

### 6. YouTube credentials

Файл `config/client_secret.json` нужно скопировать вручную после установки.

---

## 📈 Мониторинг и поддержка

### Автоматический перезапуск

Systemd автоматически перезапустит сервис при падении:
```ini
Restart=always
RestartSec=10
```

### Логирование

Два источника логов:
1. **systemd journalctl** — вывод stdout/stderr:
   ```bash
   sudo journalctl -u shorts-news -f
   ```

2. **Файловые логи** — `logs/channel_monitor.log`:
   ```bash
   tail -f logs/channel_monitor.log
   ```

### Бэкапы

Автоматические бэкапы создаются перед каждым обновлением:
```bash
/home/dzianis/bot_backups/shorts_news/
├── backup_20251102_123045/
├── backup_20251103_154521/
└── latest_backup
```

Хранятся последние 10 бэкапов.

---

## 🎯 Следующие шаги

1. ✅ **Созданы файлы развёртывания** — готовы к использованию
2. ⏭️ **Перенос на сервер** — скопировать проект на Ubuntu
3. ⏭️ **Запуск установки** — выполнить `install_ubuntu.sh`
4. ⏭️ **Настройка** — .env, ресурсы, credentials
5. ⏭️ **Тестирование** — проверка работы всех компонентов
6. ⏭️ **Мониторинг** — настройка алертов (опционально)

---

## 📚 Документация

- **UBUNTU_DEPLOYMENT.md** — полное руководство по развёртыванию
- **deploy/QUICK_DEPLOY.md** — быстрые команды и чек-листы
- **README.md** — основная документация проекта

---

## 🤝 Аналогия с существующим проектом

Ваш проект **Telegram-to-YouTube Shorts** уже работает на сервере по пути:
```
/home/dzianis/shorts-shot/
```

Новый проект **Shorts News** будет работать аналогично:
```
/home/dzianis/shorts_news/
```

Оба проекта будут:
- Запускаться как systemd сервисы
- Обновляться через `update_bot.sh`
- Логироваться через journalctl
- Иметь автоматические бэкапы
- Работать в фоновом режиме 24/7

Проекты полностью независимы и не будут конфликтовать.

